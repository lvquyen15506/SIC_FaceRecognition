# PyTorch 2.5.1 with CUDA 12.1 for NVIDIA GPUs.
# A recent NVIDIA driver is still required on the target computer.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Training and data pipeline
Pillow
matplotlib

# Regenerate the lesson report in tools/
python-docx
