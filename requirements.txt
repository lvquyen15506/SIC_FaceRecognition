# PyTorch with CUDA 12.4 for NVIDIA GPUs (including RTX 4060).
# Compatible with Python 3.13. A recent NVIDIA driver is required.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Training and data pipeline
Pillow
matplotlib

# Regenerate the lesson report in tools/
python-docx
